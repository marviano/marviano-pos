My Electron POS app is Offline-First. It uses a local SQLite database for all operations. It syncs data with a remote Backend API hosted at salespulse.cc (which uses MySQL) only when online.

Please create feature xxxx...
Make sure it works for both dev and production, use userData directory.

Fix the yyyy issue...  
This needs to work in the installed app too, use production-safe database paths.